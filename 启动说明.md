# 本地启动说明

## Mac

直接双击 `start-local.command`。脚本会自动切换到项目目录，不受终端当前所在目录影响。

也可以在终端输入：

```bash
cd "/你的项目目录"
node server.mjs
```

不能直接在 `~` 或 `/Users/mac` 中输入 `node server.mjs`，否则 Node 会去找 `/Users/mac/server.mjs`。

## Windows

双击 `start-local.bat`。

## 访问

```text
http://127.0.0.1:8787
```

如果提示端口被占用，先关闭旧的 Node 服务，再重新双击启动脚本。
